extends layout

block content
  div.container
    div.page-header
      h1(style='text-align: center;') Welcome to Mushroom Classification site.
      p(style='text-align: center;') This website use the desicion tree model. Resource: #[a(href='/decisionTree') json] #[a(href='/tree') image]
    if prediction
      if prediction == 'e'
        h1(style='color: green; text-align: center;') Your mushroom can eat.
      else
        h1(style='color: red; text-align: center;') Your mushroom is poisonous. 
    h3 Please fill the information of your mushroom. This site will tell you it can eat or be poisonous.
    div.form-group
      form(method='post')
        each data in dataDict
          label= data.name
          select.form-control(name='#{data.name}')
            each arrtibute in data.vals
              option(value='#{arrtibute.val}')= arrtibute.name
        br
        input.btn.btn-success.btn-block(type='submit')